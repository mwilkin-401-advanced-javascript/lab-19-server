'use strict';

// const Q = require('@nmq/q/server');

describe('server', () => {
  it('should serve stuff', () => {
    // const db = new Q('database');
    // expect(db).toBeAnInstanceOf(Q);
    expect('a').toEqual('a');
  });
});